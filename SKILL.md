---
name: xhs-title-skill
description: 为小红书笔记生成、改写、筛选和诊断高点击标题。用于“小红书标题”“起标题”“优化标题”“爆款标题”“标题 A/B 测试”“封面标题”，以及教程、工具、测评、故事、经历、观点随笔和口播稿。先让目标读者一眼看懂主题和关联，再用真实的痛点、冲突、具体价值或适度信息缺口制造点击欲望；不编造热点、数据、身份、效果或稀缺性。
---

# 小红书爆款标题

核心目标只有一个：根据正文，稳定产出让目标读者想点开的标题。

不要把“爆款”理解成固定句式或情绪词堆叠。标题的点击潜力取决于四件事：

`主题看得懂 × 与读者有关 × 有答案想知道 × 正文接得住`

任一项接近零，标题整体失效。

## 工作流

### 1. 找到最强点击理由

先在内部回答：

- `讲什么`：陌生读者能认出的具体对象是什么？
- `给谁看`：谁会立刻觉得“这说的是我”？
- `为什么点`：痛点、结果、反差、具体价值和未知答案中，哪一个最强？
- `正文能证明什么`：哪些数字、经历、效果、身份和比较真实存在？

至少提取 3 个不同传播单元，例如痛点、方法、结果、反差、原句或具体场景。不要先总结成一个抽象中心词，再围着它反复换句式。

### 2. 必要时只问一个问题

材料足够就直接生成。只有不同答案会明显改变标题方向时才问：

`如果读者最后只能记住一句话，你希望是哪一句？`

用户不回答时采用保守判断继续，不用追问拖慢任务。

### 3. 用传播机制生成草案

每个标题只选一个主机制，必要时加一个辅助机制：

- `痛点共鸣`：具体动作或损耗 + 解决方向。
- `数字价值`：真实数量 + 具体对象或收获。
- `对比冲突`：期待与现实、投入与结果、A 与 B 的差异。
- `认知反转`：常见看法 + 正文能论证的新判断。
- `身份场景`：真正相关的人群或时机 + 当前问题。
- `信息缺口`：先说清对象和处境，只保留原因、方法或选择中的一个答案。
- `原句压缩`：保留作者最有辨识度的短句，并补足主题锚点。

好奇缺口要适中：太模糊，读者不知道为什么关心；把答案讲完，读者没有点击必要。

用户要求“爆款公式、更多方向、多轮优化、诊断”时读取 [references/viral-hooks.md](references/viral-hooks.md)。教程、工具、产品或测评需要更多结构时读取 [references/title-patterns.md](references/title-patterns.md)。观点、随笔、社会观察和个人表达必须读取 [references/opinion-essay.md](references/opinion-essay.md)。

### 4. 动态生成并去重

根据素材量决定内部草案数量：

- 一句话或单一选题：约 12 个。
- 完整短文：约 18 个。
- 长文、多轮优化或用户要求更多：最多 24 个。

每个传播单元生成 1 至 2 个候选。按 `目标读者 + 核心承诺 + 点击理由` 聚类，同一语义最多保留 2 个；换词、换标点和换语序不算新方向。素材已经挖尽时直接说明，不为数量凑低质量标题。

默认输出 10 个时，最终候选至少覆盖 5 种传播机制，同一主机制最多保留 2 个。输出前在内部给每个标题标记主机制，超额就替换，不把标签展示给用户。

默认尽量控制在 20 个可见字符以内；这是可读性目标，不冒充平台硬规则。品牌名或英文专有名词无法自然压缩时，优先保证准确和一眼理解。

### 5. 先硬淘汰，再排序

候选先过五道硬门：

1. `一眼看懂`：陌生读者知道标题在讲什么。
2. `与我有关`：目标读者能认出自己的问题、场景或利益。
3. `值得点击`：存在具体回报、冲突或未闭合答案。
4. `正文兑现`：标题没有补造数字、结果、身份、对比或情绪。
5. `不是套话`：替换核心名词后不能套到任何热门主题。

通过后按以下顺序排序：

1. 目标读者相关性
2. 点击理由强度
3. 第一眼理解
4. 具体性与信息缺口平衡
5. 作者语言辨识度

直接删除：同义重复、营销腔、硬蹭热点、只有情绪没有主题、连续感叹号、虚构事实、恐惧或羞辱式点击。

若一个更有冲击力的判断只是从正文推导出来、正文没有明确论证，标注 `需正文补强`，不能伪装成直接可用。

## 输出

用户指定格式和数量时优先服从。

### 默认生成

输出 10 个筛选后的标题，不展示内部草案。最后给：

- `点击首选`：最值得先发的一个标题及具体理由。
- `A/B 备选`：两个只改变一个主要变量的标题，并说明测试变量。

推荐理由必须指出读者、冲突、回报或信息缺口，不说“更吸睛”“更有网感”。

### 用户要求更多

只补充尚未使用的传播单元，不重复上一批的同义版本。没有新方向时直接说明，并问一个关键问题。

### 优化已有标题

先指出最影响点击的一个问题，再给 3 至 5 个改写。保留原意和作者口吻，不补造正文没有的事实。

### A/B 测试

每组只改变一个变量，例如主题是否前置、数字是否前置、直给结论还是保留原因。用户提供真实曝光和点击数据后再迭代；不编造行业基准。

### 封面标题

用户明确要求封面时，给 5 组“封面主标题 + 笔记标题”。封面默认 6 至 12 个可见字符，负责冲突或记忆点；笔记标题补足对象和点击回报。两者承诺一致但不逐字重复。

### 只要标题

用户说“只给标题”或“不要分析”时，只输出编号标题。

## 可选目标

只有用户明确要求涨粉、个人 IP、账号定位或搜索优化时，才加载相应策略；不要让这些目标拖慢默认起标题：

- 涨粉或 IP：读取 [references/growth-objectives.md](references/growth-objectives.md)。
- 搜索、关键词或 SEO：读取 [xhs-keyword-strategy/SKILL.md](xhs-keyword-strategy/SKILL.md)。

## 真实性与运行效率

- 从真实体验和兴趣出发，细化主题。
- 不编造数据、身份、价格、效果、稀缺性、搜索量或平台背书。
- 不用与正文不符的标题、恐惧、羞辱、道德绑架或虚假夸张换点击。
- 热点必须仍然有效、与正文强相关且经过核验；否则跳过。
- 简单请求只用本文件完成；复杂或指定场景才读取一个相关参考文件，不一次加载全部资料。
- 先硬淘汰再排序，不给明显不合格的草案做完整评分。

核心能力只依赖本文件及相对路径资源，不要求第三方运行时、文件写入权限或特定操作系统。`agents/openai.yaml` 仅提供 Codex 界面元数据，其他支持 `SKILL.md` 的 Agent 可以忽略。
